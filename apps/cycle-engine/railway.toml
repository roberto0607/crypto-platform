[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/cycle-engine/Dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
